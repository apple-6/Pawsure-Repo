services:
  - type: web
    name: pawsure-backend
    env: node
    buildCommand: npm ci && npm run build
    startCommand: node dist/main.js
    envVars:
      - key: NODE_ENV
        value: production